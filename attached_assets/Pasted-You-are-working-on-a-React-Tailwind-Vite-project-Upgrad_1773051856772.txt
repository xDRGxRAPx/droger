You are working on a React + Tailwind + Vite project.

Upgrade the artistic portfolio with a full cinematic UI system inspired by Apple, Nike, and Awwwards award-winning websites.

IMPORTANT:
Do NOT break the current layout.
Do NOT remove existing components.
Only enhance the visual experience with layered animations.

The goal is to make the portfolio look like an international artist website.

---

GLOBAL OBJECTIVE

Implement a cinematic interactive experience including:

• smooth cinematic scroll
• cinematic animated background
• premium cursor
• mouse reactive light
• deep parallax hero
• film grain overlay
• scroll build animations
• interactive Nike-style typography
• animated gold typography
• cinematic loader
• artistic gallery

---

STEP 1 — INSTALL DEPENDENCIES

Install required packages:

npm install @studio-freight/lenis
npm install framer-motion

---

STEP 2 — CINEMATIC SMOOTH SCROLL

Open:

client/src/pages/Home.tsx

Add imports:

import Lenis from "@studio-freight/lenis";
import { useEffect } from "react";

Inside the Home component add:

useEffect(() => {

const lenis = new Lenis({
duration: 1.2,
smoothWheel: true
});

function raf(time) {
lenis.raf(time);
requestAnimationFrame(raf);
}

requestAnimationFrame(raf);

}, []);

---

STEP 3 — CINEMATIC BACKGROUND

Create:

client/src/components/CinematicBackground.tsx

Insert:

import { motion } from "framer-motion";

export default function CinematicBackground() {

return (

```
<div className="fixed inset-0 -z-10 overflow-hidden">

  <motion.div
    className="absolute w-[1200px] h-[1200px] rounded-full blur-[180px]"
    style={{
      background:"radial-gradient(circle, rgba(255,215,0,0.15), transparent)"
    }}
    animate={{
      x:[0,200,-200,0],
      y:[0,-150,150,0]
    }}
    transition={{
      duration:25,
      repeat:Infinity,
      ease:"linear"
    }}
  />

  <motion.div
    className="absolute w-[900px] h-[900px] rounded-full blur-[200px]"
    style={{
      background:"radial-gradient(circle, rgba(255,255,255,0.1), transparent)"
    }}
    animate={{
      x:[-200,200,-200],
      y:[100,-100,100]
    }}
    transition={{
      duration:30,
      repeat:Infinity,
      ease:"linear"
    }}
  />

</div>
```

);

}

---

STEP 4 — MOUSE REACTIVE LIGHT

Create:

client/src/components/MouseLight.tsx

Insert:

import { motion } from "framer-motion";
import { useEffect, useState } from "react";

export default function MouseLight() {

const [pos,setPos] = useState({x:0,y:0});

useEffect(()=>{

```
const move=(e)=>{
  setPos({x:e.clientX,y:e.clientY});
};

window.addEventListener("mousemove",move);

return ()=>window.removeEventListener("mousemove",move);
```

},[]);

return(

```
<motion.div
  className="fixed pointer-events-none z-40 w-[600px] h-[600px] rounded-full blur-[120px]"
  animate={{
    x:pos.x-300,
    y:pos.y-300
  }}
  style={{
    background:"radial-gradient(circle, rgba(255,215,0,0.15), transparent)"
  }}
/>
```

);

}

---

STEP 5 — CINEMATIC CURSOR

Create:

client/src/components/Cursor.tsx

Insert:

import { motion } from "framer-motion";
import { useEffect, useState } from "react";

export default function Cursor(){

const [pos,setPos]=useState({x:0,y:0});

useEffect(()=>{

```
const move=(e)=>{
  setPos({x:e.clientX,y:e.clientY});
};

window.addEventListener("mousemove",move);

return()=>window.removeEventListener("mousemove",move);
```

},[]);

return(

```
<motion.div
  className="fixed w-6 h-6 rounded-full pointer-events-none z-[9999]"
  animate={{
    x:pos.x-12,
    y:pos.y-12
  }}
  transition={{type:"spring",stiffness:500,damping:28}}
  style={{
    background:"radial-gradient(circle, rgba(255,215,0,0.8), transparent)"
  }}
/>
```

);

}

---

STEP 6 — FILM GRAIN EFFECT

Open:

client/src/index.css

Add:

.film-grain::before{
content:"";
position:fixed;
inset:0;
background:url("https://grainy-gradients.vercel.app/noise.svg");
opacity:0.15;
pointer-events:none;
z-index:50;
}

---

STEP 7 — SCROLL BUILD ANIMATION

Create:

client/src/components/ScrollBuild.tsx

Insert:

import { motion } from "framer-motion";

export default function ScrollBuild({children}){

return(

<motion.div
initial={{opacity:0,y:80,scale:0.95}}
whileInView={{opacity:1,y:0,scale:1}}
transition={{duration:1.2}}
viewport={{once:true}}

>

{children}

</motion.div>

);

}

---

STEP 8 — INTERACTIVE TYPOGRAPHY

Create:

client/src/components/InteractiveTitle.tsx

Insert:

import { motion } from "framer-motion";

export default function InteractiveTitle({children}){

return(

<motion.h1
className="text-6xl md:text-7xl font-bold gold-glow cursor-default"
whileHover={{
scale:1.05,
letterSpacing:"0.08em"
}}
transition={{duration:0.4}}

>

{children}

</motion.h1>

);

}

---

STEP 9 — GOLD ANIMATED TEXT

Add to index.css:

.gold-glow{

background:linear-gradient(90deg,#FFD700,#FFB700,#FFD700);
background-size:200%;

animation:goldFlow 6s linear infinite;

-webkit-background-clip:text;
-webkit-text-fill-color:transparent;

}

@keyframes goldFlow{

0%{background-position:0%}
100%{background-position:200%}

}

---

STEP 10 — HERO PARALLAX

Locate the hero image motion element and ensure it uses:

initial={{scale:1.25}}
animate={{scale:1}}
transition={{duration:6}}

This creates cinematic slow zoom.

---

STEP 11 — CINEMATIC LOADER

Create:

client/src/components/Loader.tsx

Insert:

import { motion } from "framer-motion";

export default function Loader(){

return(

<motion.div
className="fixed inset-0 bg-black flex items-center justify-center z-[9999]"
initial={{opacity:1}}
animate={{opacity:0}}
transition={{delay:2,duration:1}}

>

<motion.h1
className="text-4xl font-bold gold-glow"
initial={{opacity:0}}
animate={{opacity:1}}

>

D ROGER

</motion.h1>

</motion.div>

);

}

---

STEP 12 — INTEGRATE COMPONENTS

Open:

client/src/App.tsx

Add imports:

import CinematicBackground from "./components/CinematicBackground";
import Cursor from "./components/Cursor";
import MouseLight from "./components/MouseLight";
import Loader from "./components/Loader";

Render them at the top of the layout:

<CinematicBackground/>
<Cursor/>
<MouseLight/>
<Loader/>

Also ensure the root container has:

className="film-grain"

---

FINAL RESULT

The site should now include:

• cinematic smooth scroll
• animated background
• reactive light
• premium cursor
• deep hero parallax
• film grain effect
• scroll build animations
• interactive typography
• animated gold titles
• cinematic loader

This should visually resemble an international artist portfolio with cinematic experience.

Ensure everything compiles successfully.
