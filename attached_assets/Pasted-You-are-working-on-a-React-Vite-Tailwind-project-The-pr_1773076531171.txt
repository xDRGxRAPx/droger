You are working on a React + Vite + Tailwind project.

The project already has a large Home.tsx page with many sections and animations.
DO NOT rewrite or delete that file.

Your task is to upgrade the entire website with cinematic artistic effects while preserving the existing layout and structure.

IMPORTANT RULES

• Do NOT delete existing components
• Do NOT reduce the current code
• Only ADD new components and global visual layers
• Ensure everything compiles successfully

---

INSTALL REQUIRED LIBRARIES

Run:

npm install framer-motion @react-three/fiber @react-three/drei three

---

FEATURE 1 — APPLE STYLE CURSOR

Create:

client/src/components/CinematicCursor.tsx

Code:

import { useEffect, useState } from "react";

export default function CinematicCursor() {

const [pos, setPos] = useState({ x: 0, y: 0 });

useEffect(() => {

```
const move = (e: MouseEvent) => {
  setPos({ x: e.clientX, y: e.clientY });
};

window.addEventListener("mousemove", move);

return () => window.removeEventListener("mousemove", move);
```

}, []);

return (

```
<div
  className="pointer-events-none fixed z-[9999] w-40 h-40 rounded-full blur-3xl opacity-40"
  style={{
    left: pos.x - 80,
    top: pos.y - 80,
    background: "radial-gradient(circle, rgba(255,215,0,0.3), transparent)"
  }}
/>
```

);

}

---

FEATURE 2 — MOUSE LIGHT INTERACTION

Create:

client/src/components/MouseLight.tsx

Code:

import { useEffect, useState } from "react";

export default function MouseLight() {

const [pos, setPos] = useState({ x: 0, y: 0 });

useEffect(() => {

```
const move = (e: MouseEvent) => {
  setPos({ x: e.clientX, y: e.clientY });
};

window.addEventListener("mousemove", move);

return () => window.removeEventListener("mousemove", move);
```

}, []);

return (

```
<div
  className="pointer-events-none fixed inset-0 z-50"
  style={{
    background: `radial-gradient(circle at ${pos.x}px ${pos.y}px, rgba(255,255,255,0.08), transparent 400px)`
  }}
/>
```

);

}

---

FEATURE 3 — 3D PARTICLE BACKGROUND

Create:

client/src/components/ParticlesBackground.tsx

Code:

import { Canvas } from "@react-three/fiber";
import { Points, PointMaterial } from "@react-three/drei";
import { useRef } from "react";

function Particles() {

const ref = useRef();

const positions = new Float32Array(3000);

for (let i = 0; i < 3000; i++) {
positions[i] = (Math.random() - 0.5) * 10;
}

return ( <Points ref={ref} positions={positions} stride={3}> <PointMaterial
     transparent
     color="#ffffff"
     size={0.02}
     sizeAttenuation
     depthWrite={false}
   /> </Points>
);
}

export default function ParticlesBackground() {

return ( <div className="fixed inset-0 -z-10">

```
  <Canvas camera={{ position: [0, 0, 5] }}>
    <Particles />
  </Canvas>

</div>
```

);

}

---

FEATURE 4 — PARALLAX IMAGE COMPONENT

Create:

client/src/components/ParallaxImage.tsx

Code:

import { motion, useScroll, useTransform } from "framer-motion";
import { useRef } from "react";

export default function ParallaxImage({ src }: any) {

const ref = useRef(null);

const { scrollYProgress } = useScroll({
target: ref,
offset: ["start end", "end start"]
});

const y = useTransform(scrollYProgress, [0, 1], ["-120px", "120px"]);

return (

```
<div ref={ref} className="overflow-hidden">

  <motion.img
    src={src}
    style={{ y }}
    className="w-full h-[520px] object-cover"
  />

</div>
```

);

}

---

FEATURE 5 — INTERACTIVE TYPOGRAPHY

Create:

client/src/components/InteractiveTitle.tsx

Code:

import { motion } from "framer-motion";

export default function InteractiveTitle({ children }: any) {

return (

```
<motion.h1
  className="text-6xl md:text-7xl font-bold tracking-tight"
  whileHover={{
    scale: 1.05,
    letterSpacing: "0.08em"
  }}
  transition={{ duration: 0.4 }}
>

  {children}

</motion.h1>
```

);

}

---

FEATURE 6 — GLOBAL FILM GRAIN EFFECT

Open:

client/src/index.css

Add:

body::after {
content: "";
pointer-events: none;
position: fixed;
inset: 0;
opacity: 0.06;
background-image: url("https://grainy-gradients.vercel.app/noise.svg");
}

---

FEATURE 7 — ADD GLOBAL COMPONENTS

Open:

client/src/App.tsx

Import:

import CinematicCursor from "./components/CinematicCursor";
import MouseLight from "./components/MouseLight";
import ParticlesBackground from "./components/ParticlesBackground";

Add these inside the main layout wrapper BEFORE the pages render:

<ParticlesBackground />
<CinematicCursor />
<MouseLight />

---

FEATURE 8 — OPTIONAL PARALLAX USE

If there are large images in sections (hero, gallery, infrastructure), they can optionally be replaced with:

<ParallaxImage src={image} />

---

FINAL RESULT

The website must now include:

• Cinematic cursor following the mouse
• Light interaction reacting to cursor movement
• 3D particle artistic background
• Film grain cinema texture
• Parallax image movement
• Interactive typography

These elements should enhance the existing site without breaking the current layout.

Animations must remain smooth and performant.
